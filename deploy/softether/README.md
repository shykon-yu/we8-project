# SoftEther vpncmd

Put the Linux `vpncmd` executable here for the `platform-api` container:

```text
deploy/softether/vpncmd
```

The file is intentionally ignored by Git. It should come from the same
SoftEther VPN Server release that is running on the test server.
